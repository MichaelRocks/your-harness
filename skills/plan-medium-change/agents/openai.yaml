interface:
  display_name: "Plan Medium Change"
  short_description: "Clarify, save a detailed task-based plan, and implement a medium change."
  default_prompt: "Use $plan-medium-change to inspect this medium functionality request, ask clarifying questions if needed, save one plan file with a concise planned-change summary and explicit tasks, get approval, and implement the approved change while keeping task states current."

policy:
  allow_implicit_invocation: true
