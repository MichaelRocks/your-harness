interface:
  display_name: "Plan Large Change"
  short_description: "Clarify, save a phased plan set, and implement a large change."
  default_prompt: "Use $plan-large-change to inspect this large functionality request, ask clarifying questions if needed, save an overview plus phase files, get approval, and implement phase by phase unless the user explicitly approves the whole plan."

policy:
  allow_implicit_invocation: true
